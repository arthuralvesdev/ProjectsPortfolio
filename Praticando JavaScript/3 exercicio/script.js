// calcular salário por hora e tirar imposto


let calcularSalario = (hoursworked, hoursvalue) => {
    hoursworked = 180
    hoursvalue = 8
    return hoursworked * hoursvalue 
}

console.log(calcularSalario())